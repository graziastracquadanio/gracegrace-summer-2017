import { BlurWhenNeededDirective } from './blur-when-needed.directive';

describe('BlurWhenNeededDirective', () => {
  it('should create an instance', () => {
    const directive = new BlurWhenNeededDirective();
    expect(directive).toBeTruthy();
  });
});
