import { TypewritingDirective } from './typewriting.directive';

describe('TypewritingDirective', () => {
  it('should create an instance', () => {
    const directive = new TypewritingDirective();
    expect(directive).toBeTruthy();
  });
});
