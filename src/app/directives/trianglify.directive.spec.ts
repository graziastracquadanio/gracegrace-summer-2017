import { TrianglifyDirective } from './trianglify.directive';

describe('TrianglifyDirective', () => {
  it('should create an instance', () => {
    const directive = new TrianglifyDirective();
    expect(directive).toBeTruthy();
  });
});
